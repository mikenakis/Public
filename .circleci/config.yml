version: 2.1

workflows:
  my-workflow:
    jobs:
      - build:
          context: my-context

jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout









      - run: mvn --batch-mode --settings .circleci/mvn-settings.xml clean deploy
        # | grep -Ev "Download(ing|ed)"
