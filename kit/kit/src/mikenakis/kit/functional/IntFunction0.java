package mikenakis.kit.functional;

/**
 * A method which accepts no arguments and returns an {@code int} value.
 * (Corresponds to Java's {@link java.util.function.IntSupplier}.)
 *
 * @author Mike Nakis (michael.gr)
 */
public interface IntFunction0
{
	int invoke();
}
